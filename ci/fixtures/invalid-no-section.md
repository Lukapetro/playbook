Adds the CSV importer behind the `importer_v2` flag.

Closes #142.

## Summary

Ran the gates, everything passes. Branch is `feat/importer-v2`, worktree is
clean, nothing deviated from the ticket.

### Handoff

Wrong heading level, and the fields are prose rather than labels — this must
not pass.
